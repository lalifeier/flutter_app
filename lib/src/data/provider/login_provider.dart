class LoginProvider {
  LoginProvider();

  // Get request
  // Future<User> getUser() async {
  //   return User.fromJson(
  //     (await ApiConnect.instance.get(EndPoints.user)).getBody(),
  //   );
  // }
}
